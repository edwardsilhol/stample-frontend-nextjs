import { Meta } from '@storybook/react';
import WorkspaceSidebar from './WorkspaceSidebar';
export default {
  title: 'Components/sidebars/workspaceSidebar',
  component: WorkspaceSidebar,
} as Meta;

export const Default = { children: <></> };
