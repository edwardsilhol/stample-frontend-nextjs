import { Meta } from '@storybook/react';
import CircularLoading from './index';

export default {
  title: 'Components/base/CircularLoading',
  render: CircularLoading,
} as Meta;

export const Default = {};
