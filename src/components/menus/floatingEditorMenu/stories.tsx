import { Meta } from '@storybook/react';
import FloatingEditorMenu from './index';

export default {
  title: 'Components/Menus/FloatingEditorMenu',
  component: FloatingEditorMenu, // TODO: Fix this story
  args: {
    show: true,
  },
} as Meta;

export const Default = {};
