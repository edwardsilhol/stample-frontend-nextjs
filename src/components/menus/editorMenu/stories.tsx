import { Meta } from '@storybook/react';
import EditorMenu from './index';

export default {
  title: 'Components/Menus/EditorMenu',
  component: EditorMenu, // TODO: Fix this story
  args: {
    show: true,
  },
} as Meta;

export const Default = {};
